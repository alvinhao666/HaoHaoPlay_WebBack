{

    "version": "0.2.0",
    "configurations": [
        {
            "name": "启动 Chrome 并打开 localhost",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:4200",            
            "webRoot": "${workspaceRoot}"
        },
    ]
}